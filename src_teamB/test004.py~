import os
import sys
import time

try:
        while True:
            
            os.system('bto_advanced_USBIR_cmd -r')
            time.sleep(3)
            os.system('bto_advanced_USBIR_cmd -s')

            argv = sys.argv

            print("test"+str(argv))

except KeyboardInterrupt:
        pass
